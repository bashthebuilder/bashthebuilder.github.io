# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
__MACOSX/

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# LaTeX build artefacts, in case a .tex is ever worked on in here
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk
*.bbl
*.blg

# Scratch
*.bak
*.tmp

# Local working material that is not part of the published site.
# Anchored with a leading slash on purpose: core.ignorecase is true on macOS,
# so a bare "PICASO/" would also match the site's own assets/picaso/ directory.
/PICASO/

# Third-party student work: held back pending the authors' permission.
# Same leading-slash anchoring rationale as /PICASO/ above.
/Anastasia Reports/
